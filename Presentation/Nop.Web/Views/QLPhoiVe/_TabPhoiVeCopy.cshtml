@model List<Nop.Web.Models.VeXeKhach.PhoiVeCopyModel>

@foreach(var item in Model)
{
    <text>
    [@item.Id.ToString() (<a href="javascript:ChuyenVeDelete(@item.Id)" class="k-link" style="color:red;font-style:normal;">Xóa</a>)]
    </text>
}
@if(Model.Count >1)
{
    <text>
    [<a href="javascript:ChuyenVeDelete(-1)" class="k-link" style="color:red;font-style:normal;">Xóa tất cả</a>]&nbsp;
    </text>
}
